/** Shared class names for native `<select>` elements in RHF forms. */
export const formNativeSelectClassName =
  "h-11 w-full rounded-2xl border border-input bg-background/60 px-4 text-sm outline-none focus:ring-2 focus:ring-ring";
